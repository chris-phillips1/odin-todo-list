export default class TodoItem {
    constructor(title, description, dueDate, actionDate, priority) {
        this.title = title;
        this.description = description;
        this.dueDate = dueDate;
        this.actionDate = actionDate;
        this.priority = priority;
    }
}